# Forum copy (ready to paste; keep it a question, not a pitch)

## Reply for threads 79165 / 101870 / 131095
Hi all — I'm building a plugin for exactly this: user-defined typed fields
(text, number, select, rating…) stored in the Extra field so they sync safely, plus
a "Matrix" window that shows a collection as an editable table (one row per paper,
your columns) with sort/filter and Excel/Word export. Mockup: [screenshot]

Two questions before I go further:
1. Which columns would you want on day one?
2. The core will be free; would ~$25/yr (or $49 one-time) be fair for unlimited
   fields, per-column filters, saved views and DOCX/XLSX export?

Happy to add anyone here as a beta tester.

## New thread (Plugins category)
Title: LitMatrix — custom fields + literature-review matrix inside Zotero (looking for testers)

Body:
Problem: no custom fields, no table view; we all export to Excel and it goes stale.
What it does: define your own fields → edit in the item pane or as columns in the
item list → open any collection as an editable matrix → export.
Storage: `lm.key: value` lines in Extra (same approach as Zotero Reading List), so
it syncs and never touches your DB. Works alongside Better BibTeX.
Status: beta in ~2 weeks. Free core; Pro planned for unlimited fields/export.
Screenshot: [link]. Sign up for beta: [landing page].
Questions: what fields? Zotero version/OS? group libraries?

## Ze-Notes author DM (GitHub issue or email)
Hi — I love Ze-Notes' idea of a table over notes. I'm building a complementary
plugin for typed custom fields (values in Extra) with a matrix view. Would you be
open to a quick chat? Ideas: cross-linking (open Ze-Notes cell from LitMatrix row),
sharing test users, or even merging efforts. No pressure either way.
